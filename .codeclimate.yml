settings:
  import/resolver: meteor  

engines:
  eslint:
    enabled: true
    channel: "eslint-3"
    config:
      config: ".eslintrc.codeclimate.json"

  stylelint:
    enabled: true

ratings:
  paths:
    - "client/**/*"
    - "imports/**/*"
    - "server/**/*"
